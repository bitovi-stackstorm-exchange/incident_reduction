name: incident_reduction.unique_code_concurrency
description: Limits the concurrent executions for handling debounce alerts
enabled: false
resource_ref: incident_reduction.handle-alerts-debounce
policy_type: action.concurrency.attr
parameters:
    action: delay
    threshold: 1
    attributes:
        - unique_code